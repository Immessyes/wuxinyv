# 流水线名称
name: CI/CD 自动化测试流水线

# 触发条件：推送到main分支、提交PR到main分支时自动运行
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

# 任务定义
jobs:
  build-and-test:
    # 运行环境：GitHub提供的Ubuntu免费环境
    runs-on: ubuntu-latest

    steps:
      # 步骤1：拉取最新代码
      - name: 检出代码
        uses: actions/checkout@v4

      # 步骤2：配置Python 3.11环境
      - name: 设置Python环境
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: 'pip'

      # 步骤3：升级pip并安装依赖
      - name: 安装项目依赖
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # 步骤4：自动运行单元测试
      - name: 执行单元测试
        run: |
          python -m unittest discover -s tests -p "test_*.py" -v